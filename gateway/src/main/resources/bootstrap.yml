spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:7001/


server:
  port: 5555

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka/


debug: true

health:
  config:
    enabled: true
